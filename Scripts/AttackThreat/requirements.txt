# IoT Digital Twin Attack Threat Scripts - Requirements
# Cross-Platform Support: Windows, Linux/Ubuntu

# Core Python Dependencies
flask==3.0.2
telnetlib3==2.0.4
python-dotenv==1.0.1
requests==2.31.0
paho-mqtt==2.1.0
werkzeug==3.0.1
click==8.1.7
colorama>=0.4.5

# Ubuntu/Linux System Requirements:
# Run the following commands before installing Python packages:
# sudo apt-get update
# sudo apt-get install -y build-essential python3-dev libc6-dev gcc

# For Ubuntu users, run the setup script:
# python3 ubuntu_setup.py

# Windows users can use the pre-compiled extensions directly.

# C Extension Compilation (Ubuntu/Linux):
# python3 setup_fast_bruteforce_cross.py build_ext --inplace
# python3 setup_fast_ddos_cross.py build_ext --inplace
