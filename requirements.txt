# IoT Digital Twin Security Research Platform - Dependencies

# Core dependencies
requests>=2.25.0
flask>=2.0.0
flask-socketio>=5.0.0

# Optional dependencies for enhanced functionality
colorama>=0.4.4        # For colored terminal output
tabulate>=0.8.9        # For formatted table display

# Development dependencies (optional)
# pytest>=6.0.0        # For testing (uncomment if needed)
# flake8>=3.8.0        # For code linting (uncomment if needed)

# Note: Most functionality uses Python standard library
# Additional dependencies are kept minimal for security research compatibility
